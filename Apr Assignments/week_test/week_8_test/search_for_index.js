var arr= ['a','b','c','d'];
for(let i=0;i<arr.length;i++){
    if(arr[i]==='b'){
        console.log(`index of b is ${i}`); //output - index of b is 1
    }
    // if(arr[i]==b){
    //     console.log(`index of b is ${i}`); outputs - undefined value of b
    // }
    //we should be careful while declaring and while using it in the code there is a lot of difference 'b' and b.
}